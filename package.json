{
  "name": "mobx-persist",
  "version": "0.0.6",
  "description": "create and persist mobx stores",
  "main": "index.js",
  "typings:": "index.d.ts",
  "scripts": {
    "build": "tsc && shx cp package.json .build/ && shx cp LICENSE.md .build/ && shx cp README.md .build/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/pinqy520/mobx-persist.git"
  },
  "keywords": [
    "mobx",
    "store",
    "persist",
    "mobx-persist"
  ],
  "author": "Huang Qi",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/pinqy520/mobx-persist/issues"
  },
  "homepage": "https://github.com/pinqy520/mobx-persist#readme",
  "dependencies": {
    "serializr": "^1.1.7"
  },
  "devDependencies": {
    "mobx": "^2.5.1",
    "shx": "^0.1.4"
  }
}
